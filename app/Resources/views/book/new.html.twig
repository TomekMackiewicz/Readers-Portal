{% extends 'base.html.twig' %}

{% block body %}
  <div class="row">
  	<div class="col-md-3"></div>
    <div class="col-md-6">	
	    <h1>New Book</h1>

	    {{ form_start(form) }}
	        {{ form_widget(form) }}
	        <input type="submit" value="Create" class="btn btn-default" />
	    {{ form_end(form) }}

	    <a href="{{ path('book_index') }}" class="btn btn-default">Back to the list</a>
		</div>
		<div class="col-md-3"></div>
	</div>
{% endblock %}
