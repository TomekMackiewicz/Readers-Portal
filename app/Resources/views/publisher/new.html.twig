{% extends 'base.html.twig' %}

{% block body %}
    <h1>Publisher creation</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Create" class="btn btn-default" />
    {{ form_end(form) }}

    <a href="{{ path('publisher_index') }}" class="btn btn-default">Back to the list</a>

{% endblock %}
